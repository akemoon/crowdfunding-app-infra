apiVersion: v1
kind: Service
metadata:
  name: project-svc
  namespace: back
  labels:
    app: project
spec:
  selector:
    app: project
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
